# Bridge configuration
# All keys are required (no hardcoded defaults in code)

app_id: bridge
provider: openai
model: gpt-4.1-mini
temperature: 0.7
system_prompt: ""

# context_window: null means "resolve from hub model specs"
# Set explicitly to override (e.g. 128000)
# context_window:
